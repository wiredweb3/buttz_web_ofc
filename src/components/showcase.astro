---
import ContentSection from "~/components/content-section.astro";
import ShowcaseCard from "~/components/showcase-card.astro";
import { getShowcase } from "~/data/showcase";
import { Picture } from "@astrojs/image/components";
import { Icon } from "astro-icon";

import banner from "~/assets/pics/dedust.png"
import banner2 from "~/assets/pics/gaspump.png"
import banner3 from "~/assets/pics/stonfi.png"


const sites = await getShowcase();
const widths=[1000];
const sizes = "(max-width: 280px) 21vw, 21vw";
---

<ContentSection title="Trade and swap $BUTTZ" id="swap">
  <Fragment slot="lead">
    After launch, <span class="text-primary">$BUTTZ</span> will be available for trading on <span class="text-primary">DeDust, Ston.fi</span> and all others main Base DEX.


<br>Running on TON blockchain, <span class="text-primary">DeDust</span> and <span class="text-primary">Ston.fi</span> are AMM's and decentralized exchanges that ensure an healthier and safe trading environment.

<div class="max-w-4xl space-y-2">
  <div class="grid grid-cols-3 gap-4 md:grid-cols-3 lg:grid-cols-3">
      <a href="https://DeDust.io" target="_blank">
      <Picture
      src={banner}
      widths={widths}
      alt="$BUTTZ on DeDust"
    />

      <a href="https://gaspump.fun" target="_blank">
      <Picture
      src={banner2}
      widths={widths}
      alt="$BUTTZ on GasPump"
    />
      
      <a href="https://DeDust.io" target="_blank">
      <Picture
      src={banner3}
      widths={widths}
      alt="$BUTTZ on Ston.fi"
    />
  </div>
</div>

</Fragment>
</ContentSection>
